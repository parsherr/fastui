---
title: useScrollPosition
date: 2025-05-05
description: Tracks the current scroll position of the page.
author: h3rmel
published: true
---

<HookPreview name="use-scroll-position-demo" />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add "https://guarahooks.com/r/use-scroll-position"
```

</TabsContent>
<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<HookSource name="use-scroll-position" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>
</Tabs>

## Props

| Prop        | Type                             | Default     | Description                                                  |
| ----------- | -------------------------------- | ----------- | ------------------------------------------------------------ |
| `onChange?` | `(x: number, y: number) => void` | `undefined` | A callback function called when the scroll position changes. |

## Data

| Prop | Type     | Description                        |
| ---- | -------- | ---------------------------------- |
| `x`  | `number` | The horizontal scroll offset (px). |
| `y`  | `number` | The vertical scroll offset (px).   |

## Key Features & Details

### SSR & Initial Values

- The hook checks if it's running in a browser. On the server, it returns `0` for both `x` and `y`.
- On mount, the hook initializes the scroll position based on the current `window.scrollX` and `window.scrollY`.

### Passive Scroll Listener

- The scroll listener is registered with `{ passive: true }` for better performance.
- The listener is cleaned up on unmount to prevent memory leaks.

## Examples

### Basic Usage

```tsx
const { x, y } = useScrollPosition();
return (
  <div>
    Scroll position: {x} x {y}
  </div>
);
```

### With onChange Callback

```tsx
useScrollPosition({
  onChange: (x, y) => {
    console.log('Scroll position:', x, y);
  },
});
```
